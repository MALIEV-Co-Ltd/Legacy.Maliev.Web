@using System.Globalization

<section class="service-section service-section-soft service-location-section" aria-labelledby="service-location-title" data-migration-component="service-location">
    <div class="service-container service-split">
        <div class="service-copy">
            <p class="service-eyebrow">@T("Verified workshop and contact", "สถานที่และช่องทางติดต่อที่ยืนยันแล้ว")</p>
            <h2 id="service-location-title">
                @if (IsThai)
                {
                    <text>บริการผลิตชิ้นงานจาก<span class="maliev-nowrap">ปากเกร็ด</span> นนทบุรี</text>
                }
                else
                {
                    <text>Manufacturing support from <span class="maliev-nowrap">Pak Kret</span>, Nonthaburi</text>
                }
            </h2>
            <p>@T(
                "MALIEV supports customers in Bangkok and Nonthaburi and reviews projects across Thailand. Files and project details can be sent online; delivery and onsite work depends on service and project review.",
                "MALIEV ให้บริการลูกค้าในกรุงเทพและนนทบุรี พร้อมประเมินโครงการจากทั่วประเทศไทย สามารถส่งไฟล์และรายละเอียดออนไลน์ โดยการจัดส่งและงานนอกสถานที่ขึ้นกับประเภทบริการและการประเมินโครงการ")</p>
            <address>
                <strong>Maliev Co., Ltd.</strong><br />
                @if (IsThai)
                {
                    <text>36/1 หมู่ 3 ตำบลคลองข่อย อำเภอ<span class="maliev-nowrap">ปากเกร็ด</span> จังหวัดนนทบุรี 11120 ประเทศไทย</text>
                }
                else
                {
                    <text>36/1 Moo 3, Khlong Khoi, <span class="maliev-nowrap">Pak Kret</span>, Nonthaburi 11120, Thailand</text>
                }
            </address>
            <p>
                <strong>@T("Business hours", "เวลาทำการ")</strong><br />
                @T("Monday-Friday: 09:00-18:00", "จันทร์-ศุกร์: 09:00-18:00 น.")<br />
                @T("Saturday-Sunday: Closed", "เสาร์-อาทิตย์: ปิด")
            </p>
            <div class="service-actions">
                <a class="service-button service-button-primary" href="@Legacy.Maliev.Web.Application.SocialNetworks.GoogleMaps" target="_blank" rel="noopener"><i class="fas fa-map-marked-alt" aria-hidden="true"></i><span>@T("Open Google Maps", "เปิด Google Maps")</span></a>
                <a class="service-button service-button-secondary" href="@Legacy.Maliev.Web.Application.SocialNetworks.Line" target="_blank" rel="noopener" data-contact-placement="service_location_line"><i class="fab fa-line" aria-hidden="true"></i><span>@T("Chat on LINE OA", "ติดต่อผ่าน LINE OA")</span></a>
                <a class="service-button service-button-secondary" href="/quotation" data-contact-placement="service_location_quotation"><i class="fas fa-file-upload" aria-hidden="true"></i><span>@T("Send files for review", "ส่งไฟล์เพื่อประเมิน")</span></a>
            </div>
        </div>
    </div>
</section>

@code {
    private bool IsThai => CultureInfo.CurrentCulture.TwoLetterISOLanguageName == "th";

    private string T(string english, string thai) => IsThai ? thai : english;
}
