@using Microsoft.AspNetCore.Components
@inject IStringLocalizer<EmailConfirmationContent> Localizer

<main class="maliev-page maliev-page-main" data-migration-component="email-confirmation-content" data-migration-renderer="blazor-static-ssr">
    <div class="maliev-container">
        <section class="maliev-panel maliev-status">
            <p class="maliev-eyebrow">@Localizer["Email confirmation"]</p>
            <h1>@Localizer["We could not confirm your email"]</h1>
            <p>@Localizer["Request a new confirmation email or contact support."]</p>
            <div class="validation-summary-errors @(Model.Errors.Count > 0 ? string.Empty : "validation-summary-valid")" role="alert" data-valmsg-summary="true">
                <ul>
                    @foreach (var error in Model.Errors)
                    {
                        <li>@Localizer[error]</li>
                    }
                </ul>
            </div>
            <a href="/Account/Login" class="maliev-button">@Localizer["Back to sign in"]</a>
        </section>
    </div>
</main>

@code {
    [Parameter, EditorRequired]
    public EmailConfirmationDisplayModel Model { get; set; } = null!;
}
